{
  "hooks": {
    "PermissionRequest": [
      {
        "hooks": [
          {
            "type": "mcp_tool",
            "server": "demo-server",
            "tool": "notify",
            "if": "Bash(git *)"
          }
        ],
        "matcher": "mcp__.*"
      }
    ]
  }
}
