{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "verify: {{PostToolUse}}",
            "if": "Bash(git *)"
          }
        ],
        "matcher": "mcp__.*"
      }
    ]
  }
}
